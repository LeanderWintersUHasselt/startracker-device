[core]
shell=kiosk-shell.so
idle-time=0

# No [output] section: Weston auto-detects the connected display.
# kiosk-shell (Weston 14) does not support a client= launch directive;
# startracker-ui is started separately from ~/.bash_profile on tty1.
